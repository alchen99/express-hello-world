apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: hello-blue-world-httpd
spec:
  replicas: 1
  template:
    metadata:
      labels: # labels to select/identify the deployment
        app: hello-blue-world-httpd  
    spec:     # pod spec                  
      containers: 
      - name: hello-blue-world-httpd 
        image: alchen99/express-hello-world:alpine-httpd-blue # image we pushed
        ports:
        - containerPort: 80
