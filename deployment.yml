apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: hello-green-world-httpd
spec:
  replicas: 1
  template:
    metadata:
      labels: # labels to select/identify the deployment
        app: hello-green-world-httpd  
    spec:     # pod spec                  
      containers: 
      - name: hello-green-world-httpd 
        image: alchen99/express-hello-world:alpine-httpd-green # image we pushed
        ports:
        - containerPort: 80
